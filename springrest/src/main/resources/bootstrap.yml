info:
  app:
    name: Rest service
    description: Rest
    version: 2

spring:
  application:
    name: rest-service
  jmx:
    default-domain: ${spring.application.name}
    enabled: false
  profiles:
    active: development
